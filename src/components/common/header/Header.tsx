import Navbar from "./Navbar";
import { StyledHeader } from "../../../styles/layout/header";

const Header = () => {
  return (
    <StyledHeader>
      <Navbar />
    </StyledHeader>
  );
};

export default Header;
